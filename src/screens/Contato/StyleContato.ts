import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex:1,
        padding:20
    },

    titulo: {
        fontSize:32,
        fontWeight:"bold",
        textAlign:"center",
        marginBottom:30
    },

    cards: {
        alignItems:"center"
    }
});